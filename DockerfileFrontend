# Usa una imagen ligera de Nginx
FROM nginx:alpine

# Copia todos los archivos de la carpeta 'frontend'
# al directorio por defecto de Nginx para archivos estáticos
COPY . /usr/share/nginx/html

# Expone el puerto 80, que es el que Nginx usará
EXPOSE 80

# El comando por defecto de Nginx ya está configurado, no necesitas un CMD

